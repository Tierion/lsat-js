/**
 * @description Given a string, determine if it is in hex encoding or not.
 * @param {string} h - string to evaluate
 */
export declare function isHex(h: string): boolean;
export declare function decode(req: string): any;
export declare function getIdFromRequest(req: string): string;
